
'use strict';

const assert = require('assert');

const formula = require('../util/formula');

describe('util/formula.js', () => {
  describe('#compoundRate(interest, period)', () => {
    it('should return interest when period is 1', () => {
      assert.equal(formula.compoundRate(9.6, 1), 9.6);
      assert.equal(formula.compoundRate(6, 1), 6.0);
      assert.equal(formula.compoundRate(6, 1), 6.0);
    });

    it('should return correct interest when period 3, 6, 12, 18', () => {
      assert.equal(formula.compoundRate(6.27, 3), 6.3);
      assert.equal(formula.compoundRate(7.49, 6), 7.6);
      assert.equal(formula.compoundRate(8.28, 12), 8.6);
      assert.equal(formula.compoundRate(8.48, 18), 9.0);
    });
    it('should return correct interest when period is 6', () => {
      assert.equal(formula.compoundRate(4.2, 6), 4.2);
      assert.equal(formula.compoundRate(4.3, 6), 4.3);
      assert.equal(formula.compoundRate(4.4, 6), 4.4);
      assert.equal(formula.compoundRate(4.5, 6), 4.5);
      assert.equal(formula.compoundRate(4.6, 6), 4.6);
      assert.equal(formula.compoundRate(4.7, 6), 4.7);
      assert.equal(formula.compoundRate(4.8, 6), 4.8);
      assert.equal(formula.compoundRate(4.9, 6), 4.9);
      assert.equal(formula.compoundRate(5.0, 6), 5.0);
      assert.equal(formula.compoundRate(5.1, 6), 5.1);
      assert.equal(formula.compoundRate(5.2, 6), 5.2);
      assert.equal(formula.compoundRate(5.3, 6), 5.3);
      assert.equal(formula.compoundRate(5.4, 6), 5.4);
      assert.equal(formula.compoundRate(5.5, 6), 5.5);
      assert.equal(formula.compoundRate(5.6, 6), 5.6);
      assert.equal(formula.compoundRate(5.7, 6), 5.7);
      assert.equal(formula.compoundRate(5.8, 6), 5.8);
      assert.equal(formula.compoundRate(5.9, 6), 5.9);
      assert.equal(formula.compoundRate(6.0, 6), 6.0);
      assert.equal(formula.compoundRate(6.1, 6), 6.1);
      assert.equal(formula.compoundRate(6.2, 6), 6.2);
      assert.equal(formula.compoundRate(6.3, 6), 6.3);
      assert.equal(formula.compoundRate(6.4, 6), 6.4);
      assert.equal(formula.compoundRate(6.5, 6), 6.5);
      assert.equal(formula.compoundRate(6.6, 6), 6.6);
      assert.equal(formula.compoundRate(6.7, 6), 6.7);
      assert.equal(formula.compoundRate(6.8, 6), 6.8);
      assert.equal(formula.compoundRate(6.9, 6), 6.9);
      assert.equal(formula.compoundRate(7.0, 6), 7.1);
      assert.equal(formula.compoundRate(7.1, 6), 7.2);
      assert.equal(formula.compoundRate(7.2, 6), 7.3);
      assert.equal(formula.compoundRate(7.3, 6), 7.4);
      assert.equal(formula.compoundRate(7.4, 6), 7.5);
      assert.equal(formula.compoundRate(7.5, 6), 7.6);
      assert.equal(formula.compoundRate(7.6, 6), 7.7);
      assert.equal(formula.compoundRate(7.7, 6), 7.8);
      assert.equal(formula.compoundRate(7.8, 6), 7.9);
      assert.equal(formula.compoundRate(7.9, 6), 8.0);
      assert.equal(formula.compoundRate(8.0, 6), 8.1);
      assert.equal(formula.compoundRate(8.1, 6), 8.2);
      assert.equal(formula.compoundRate(8.2, 6), 8.3);
      assert.equal(formula.compoundRate(8.3, 6), 8.4);
      assert.equal(formula.compoundRate(8.4, 6), 8.5);
      assert.equal(formula.compoundRate(8.5, 6), 8.6);
      assert.equal(formula.compoundRate(8.6, 6), 8.7);
      assert.equal(formula.compoundRate(8.7, 6), 8.8);
      assert.equal(formula.compoundRate(8.8, 6), 8.9);
      assert.equal(formula.compoundRate(8.9, 6), 9.0);
      assert.equal(formula.compoundRate(9.0, 6), 9.1);
      assert.equal(formula.compoundRate(9.1, 6), 9.2);
      assert.equal(formula.compoundRate(9.2, 6), 9.3);
      assert.equal(formula.compoundRate(9.3, 6), 9.4);
      assert.equal(formula.compoundRate(9.4, 6), 9.5);
      assert.equal(formula.compoundRate(9.5, 6), 9.6);
      assert.equal(formula.compoundRate(9.6, 6), 9.7);
      assert.equal(formula.compoundRate(9.7, 6), 9.8);
      assert.equal(formula.compoundRate(9.8, 6), 10.0);
      assert.equal(formula.compoundRate(9.9, 6), 10.1);
      assert.equal(formula.compoundRate(10.0, 6), 10.2);
      assert.equal(formula.compoundRate(10.1, 6), 10.3);
      assert.equal(formula.compoundRate(10.2, 6), 10.4);
      assert.equal(formula.compoundRate(10.3, 6), 10.5);
      assert.equal(formula.compoundRate(10.4, 6), 10.6);
      assert.equal(formula.compoundRate(10.5, 6), 10.7);
      assert.equal(formula.compoundRate(10.6, 6), 10.8);
      assert.equal(formula.compoundRate(10.7, 6), 10.9);
      assert.equal(formula.compoundRate(10.8, 6), 11.0);
      assert.equal(formula.compoundRate(10.9, 6), 11.1);
      assert.equal(formula.compoundRate(11.0, 6), 11.2);
      assert.equal(formula.compoundRate(11.1, 6), 11.3);
      assert.equal(formula.compoundRate(11.2, 6), 11.4);
      assert.equal(formula.compoundRate(11.3, 6), 11.5);
      assert.equal(formula.compoundRate(11.4, 6), 11.6);
      assert.equal(formula.compoundRate(11.5, 6), 11.7);
      assert.equal(formula.compoundRate(11.6, 6), 11.8);
      assert.equal(formula.compoundRate(11.7, 6), 11.9);
      assert.equal(formula.compoundRate(11.8, 6), 12.0);
      assert.equal(formula.compoundRate(11.9, 6), 12.1);
      assert.equal(formula.compoundRate(12.0, 6), 12.3);
      assert.equal(formula.compoundRate(12.1, 6), 12.4);
      assert.equal(formula.compoundRate(12.2, 6), 12.5);
      assert.equal(formula.compoundRate(12.3, 6), 12.6);
      assert.equal(formula.compoundRate(12.4, 6), 12.7);
      assert.equal(formula.compoundRate(12.5, 6), 12.8);
      assert.equal(formula.compoundRate(12.6, 6), 12.9);
      assert.equal(formula.compoundRate(12.7, 6), 13.0);
      assert.equal(formula.compoundRate(12.8, 6), 13.1);
      assert.equal(formula.compoundRate(12.9, 6), 13.2);
      assert.equal(formula.compoundRate(13.0, 6), 13.3);
      assert.equal(formula.compoundRate(13.1, 6), 13.4);
      assert.equal(formula.compoundRate(13.2, 6), 13.5);
      assert.equal(formula.compoundRate(13.3, 6), 13.6);
      assert.equal(formula.compoundRate(13.4, 6), 13.7);
      assert.equal(formula.compoundRate(13.5, 6), 13.8);
      assert.equal(formula.compoundRate(13.6, 6), 13.9);
      assert.equal(formula.compoundRate(13.7, 6), 14.0);
      assert.equal(formula.compoundRate(13.8, 6), 14.2);
      assert.equal(formula.compoundRate(13.9, 6), 14.3);
      assert.equal(formula.compoundRate(14.0, 6), 14.4);
      assert.equal(formula.compoundRate(14.1, 6), 14.5);
      assert.equal(formula.compoundRate(14.2, 6), 14.6);
      assert.equal(formula.compoundRate(14.3, 6), 14.7);
      assert.equal(formula.compoundRate(14.4, 6), 14.8);
      assert.equal(formula.compoundRate(14.5, 6), 14.9);
      assert.equal(formula.compoundRate(14.6, 6), 15.0);
    });
  });
});
